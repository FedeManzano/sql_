

GO
CREATE FUNCTION f_Validar_Fecha (@FECHA VARCHAR(20))
RETURNS INT
AS
BEGIN
	RETURN
	(
		SELECT
			CASE 
				WHEN TRY_CONVERT(DATE, @FECHA) IS NOT NULL THEN 1
				ELSE 0
			END AS Result
	)
END
GO